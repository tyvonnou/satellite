package model.strategie.deplacement;

import model.ElementMobile;
import model.strategie.Strategie;

public class DeplacementBalise extends Strategie implements Deplacement
{

  @Override
  public void bouge(ElementMobile cible)
  {
    this.bouge(cible);
  }
}
